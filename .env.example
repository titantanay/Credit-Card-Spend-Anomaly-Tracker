# Copy to .env and adjust as needed. Do not commit .env.

# Absolute or relative path to the DuckDB warehouse file
# DUCKDB_PATH=database/spend_monitor.duckdb

# Local Ollama endpoint and model (dashboard works without Ollama)
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=gemma2:2b
